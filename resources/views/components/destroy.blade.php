<a class="btn btn-danger icon-btn md-btn-flat product-tooltip"
   href="{{$href_destroy}}" onclick="return confirm('Are you sure you want to delete this record?')"
   title="{{ __('generic.delete') }}">
    Delete
</a>